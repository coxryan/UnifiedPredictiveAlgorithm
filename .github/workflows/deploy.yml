import React from "react";

const StatusTab: React.FC = () => {
  const v = `?v=${Date.now()}`;

  return (
    <div>
      {/* ... other content ... */}

      <div className="note" style={{ marginTop: 8 }}>
        <a href={`data/team_inputs.csv${v}`} target="_blank" rel="noreferrer">
          team inputs CSV ↗
        </a>
        {" • "}
        <a href={`data/schedule.csv${v}`} target="_blank" rel="noreferrer">
          schedule CSV ↗
        </a>
        {" • "}
        <a href={`data/predictions.csv${v}`} target="_blank" rel="noreferrer">
          predictions CSV ↗
        </a>
        {" • "}
        <a href={`data/live_edge.csv${v}`} target="_blank" rel="noreferrer">
          live edge CSV ↗
        </a>
        {" • "}
        <a href={`data/backtest_summary_2024.csv${v}`} target="_blank" rel="noreferrer">
          backtest summary (2024) CSV ↗
        </a>
        {" • "}
        <a href={`data/market_unmatched.csv${v}`} target="_blank" rel="noreferrer">
          market unmatched CSV ↗
        </a>
        {" • "}
        <a href={`data/backtest_predictions_2024.csv${v}`} target="_blank" rel="noreferrer" alt="backtest predictions (2024) CSV">
          backtest predictions (2024) CSV ↗
        </a>
      </div>
    </div>
  );
};

export default StatusTab;